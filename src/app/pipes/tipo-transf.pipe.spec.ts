import { TipoTransfPipe } from './tipo-transf.pipe';

describe('TipoTransfPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoTransfPipe();
    expect(pipe).toBeTruthy();
  });
});
